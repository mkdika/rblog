doctype html
html
  head
    title 
      ' rBlog |
      = area :title, "..."
    meta name="viewport" content="width=device-width, initial-scale=1"
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
    = favicon_link_tag asset_path('ruby.png')

  body
  flash[:recaptcha_error]
    - if flash[:notice]
      .notification.is-primary.is-full.has-text-centered.is-size-7-mobile.is-size-6-desktop
        button.delete
        = flash[:notice]
    - if flash[:recaptcha_error]
      .notification.is-warning.is-full.has-text-centered.is-size-7-mobile.is-size-6-desktop
        button.delete
        = flash[:recaptcha_error]
    - if alert
      .notification.is-warning.is-full.has-text-centered.is-size-7-mobile.is-size-6-desktop
        button.delete
        = alert
    = yield